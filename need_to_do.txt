
- new_asset в modal
- фильтры
- вывод всех справочников в разделе dictionaries;
- возможность редактирования в dictionaries, как в assets;


Опционально:

- странички в табличках assets, dict_users, 
- аутентификация
- my_assets (берет assets, зарегистрированные под пользователеом, под кем залогинились)

Вопросы:
- формат времени, (!) js календарь,
- размер страницы asset меньше ,чем остальных (плывет)
- footer косой в new_dict_user.html
- накладывается фон в таблице на границу conteiner-body из-за hover
- передать организорвать поиск (передать input в view)

- зачем нужны forms


Комментарии
- сортировка по алфавиту;
- (!) при удалении user - удяляеться device;





done:
+ вывод красивой таблички с базы данных в assets;
+ кнопочки "add", "remove", "change" на страничке assets;
+ radio buttons на против каждого поля в табличке assets (для возможности "add", "remove", "change" над выбранным элементом)
	или hover, появляющеюся две кнопочки для удаления или редактирования конкретного элемента
+ страничка new_asset и запись в базу данных всех полей;
+ страничка contact с моими контактами
+ сбрасывает user при edit_asset
+ редактирование edit_asset